BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Quiz] ADD [repeatCorrectQuestions] BIT NOT NULL CONSTRAINT [Quiz_repeatCorrectQuestions_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
