BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Quiz] ADD [feedbackVisibility] NVARCHAR(1000) NOT NULL CONSTRAINT [Quiz_feedbackVisibility_df] DEFAULT 'full';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
